# Default Checked in React (for checkboxes)

If i created a useState()
const [Name, setName] = useState(false); // by default its initialized false

Assign (defaultChecked)
<input type="" defaultChecked={Name}></input>

